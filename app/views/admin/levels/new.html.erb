<% provide(:title, "New Level")%>
<h1>New Level</h1>
<div class="row">
  <div class="span6 offset3">
    <%= form_for [:admin, @level] do |f| %>
	  <%= f.label :name %>
	  <%= f.text_field :name %>
	  <%= f.submit "Create new level", class: "btn btn-large btn-primary" %>
	<% end %>
  </div>
</div>
