<% provide(:title, "Subject New")%>
<h1>Subject new</h1>
<div class="row">
  <div class="span6 offset3">
    <%= form_for [:admin, @subject] do |f| %>
	  <%= f.label :name %>
	  <%= f.text_field :name %>
	  <%= f.submit "Create new Subject", class: "btn btn-large btn-primary" %>
	<% end %>
  </div>
</div>
